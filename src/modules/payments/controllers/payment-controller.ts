import type{Request,Response}from'express';import{z}from'zod';import{readCookie}from'../../../shared/http/cookie-reader.ts';import{AppError}from'../../../shared/http/errors.ts';import type{AuthenticatedActorService}from'../../../shared/security/authenticated-actor.ts';import{checkoutInputSchema}from'../dto/payment-input.ts';import type{PaymentService}from'../services/payment-service.ts';
export class PaymentController{readonly #service:PaymentService;readonly #actors:AuthenticatedActorService;constructor(service:PaymentService,actors:AuthenticatedActorService){this.#service=service;this.#actors=actors;}checkout=async(req:Request,res:Response)=>{const actor=this.#actors.authorizeUnsafe(readCookie(req,'access_token')??undefined,req.header('x-csrf-token'));const parsed=checkoutInputSchema.safeParse(req.body);if(!parsed.success)throw new AppError(422,'VALIDATION_ERROR','Validation failed.');res.status(201).json({success:true,message:'Payment checkout created.',data:await this.#service.checkout(actor.userPublicId,parsed.data)});};notification=async(req:Request,res:Response)=>{res.json({success:true,message:'Payment notification accepted.',data:await this.#service.notification(req.body)});};reconcile=async(req:Request,res:Response)=>{const actor=this.#actors.authorizeUnsafe(readCookie(req,'access_token')??undefined,req.header('x-csrf-token'));const id=this.#id(req);res.json({success:true,message:'Payment reconciled.',data:await this.#service.reconcile(actor.userPublicId,id)});};subscription=async(req:Request,res:Response)=>{const actor=this.#actors.authenticate(readCookie(req,'access_token')??undefined);res.json({success:true,message:'Current subscription retrieved.',data:await this.#service.currentSubscription(actor.userPublicId)});};list=async(req:Request,res:Response)=>{const actor=this.#actors.authenticate(readCookie(req,'access_token')??undefined);res.json({success:true,message:'Payments retrieved.',data:await this.#service.list(actor.userPublicId)});};get=async(req:Request,res:Response)=>{const actor=this.#actors.authenticate(readCookie(req,'access_token')??undefined);res.json({success:true,message:'Payment retrieved.',data:await this.#service.get(actor.userPublicId,this.#id(req))});};#id(req:Request){const id=z.uuid().safeParse(req.params.publicId);if(!id.success)throw new AppError(422,'VALIDATION_ERROR','Validation failed.');return id.data;}}
