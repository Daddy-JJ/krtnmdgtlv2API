import type { Pool, ResultSetHeader, RowDataPacket } from 'mysql2/promise';
import { randomUUID } from 'node:crypto';
import type { CheckoutAuthority, CurrentSubscription, NotificationOutcome, PaymentNotification, PaymentRecord, PaymentRepository } from './payment-repository.ts';

type AuthorityRow = RowDataPacket & { user_id: number; email: string; full_name: string; current_plan_code: 'starter'|'basic'|'pro'; code: 'basic'|'pro'; name: string; price_amount: number|string; currency: string; duration_days: number };
type PaymentRow = RowDataPacket & { public_id: string; merchant_order_id: string; target_plan_code: string; plan_name_snapshot: string; duration_days_snapshot: number; amount: number|string; currency: string; status: string; gateway_status: string|null; snap_redirect_url: string|null; paid_at: Date|null; expires_at: Date|null; created_at: Date };
const payment = (row: PaymentRow): PaymentRecord => ({ publicId: row.public_id, merchantOrderId: row.merchant_order_id, targetPlanCode: row.target_plan_code, planName: row.plan_name_snapshot, durationDays: row.duration_days_snapshot, amount: Number(row.amount), currency: row.currency, status: row.status, gatewayStatus: row.gateway_status, redirectUrl: row.snap_redirect_url, paidAt: row.paid_at, expiresAt: row.expires_at, createdAt: row.created_at });
const columns = `p.public_id,p.merchant_order_id,p.target_plan_code,p.plan_name_snapshot,p.duration_days_snapshot,p.amount,p.currency,p.status,p.gateway_status,p.snap_redirect_url,p.paid_at,p.expires_at,p.created_at`;

export class MySqlPaymentRepository implements PaymentRepository {
  readonly #pool: Pool;
  constructor(pool: Pool) { this.#pool = pool; }

  async findCheckoutAuthority(userPublicId: string, targetPlanCode: 'basic'|'pro'): Promise<CheckoutAuthority|null> {
    const [rows] = await this.#pool.execute<AuthorityRow[]>(`SELECT u.id user_id,u.email,COALESCE(cc.full_name,SUBSTRING_INDEX(u.email,'@',1)) full_name,p.code,p.name,p.price_amount,p.currency,p.duration_days,COALESCE((SELECT sp.code FROM subscriptions s JOIN plans sp ON sp.id=s.plan_id WHERE s.user_id=u.id AND s.status='active' AND s.starts_at<=UTC_TIMESTAMP() AND s.ends_at>UTC_TIMESTAMP() ORDER BY s.ends_at DESC LIMIT 1),'starter') current_plan_code FROM users u JOIN cards c ON c.user_id=u.id AND c.deleted_at IS NULL AND c.status<>'deleted' LEFT JOIN card_contacts cc ON cc.card_id=c.id JOIN plans p ON p.code=? AND p.is_active=1 WHERE u.public_id=? AND u.status='active' AND u.email_verified_at IS NOT NULL LIMIT 1`, [targetPlanCode,userPublicId]);
    const row=rows[0]; if(!row)return null;
    return { userId:row.user_id,email:row.email,fullName:row.full_name,currentPlanCode:row.current_plan_code,targetPlan:{code:row.code,name:row.name,amount:Number(row.price_amount),currency:row.currency,durationDays:row.duration_days} };
  }

  async insertPending(input:{publicId:string;merchantOrderId:string;authority:CheckoutAuthority;now:Date}):Promise<PaymentRecord>{const a=input.authority;await this.#pool.execute(`INSERT INTO payments(public_id,user_id,gateway,merchant_order_id,target_plan_code,plan_name_snapshot,duration_days_snapshot,amount,currency,status,created_at,updated_at) VALUES(?,?,'midtrans',?,?,?,?,?,?,'pending',?,?)`,[input.publicId,a.userId,input.merchantOrderId,a.targetPlan.code,a.targetPlan.name,a.targetPlan.durationDays,a.targetPlan.amount,a.targetPlan.currency,input.now,input.now]);const result=await this.findOwnedByPublicId(input.publicId);if(!result)throw new Error('Inserted payment could not be read.');return result;}
  async attachGatewayCheckout(publicId:string,redirectUrl:string,expiresAt:Date|null,now:Date):Promise<PaymentRecord|null>{await this.#pool.execute<ResultSetHeader>(`UPDATE payments SET snap_redirect_url=?,expires_at=?,gateway_status='pending',updated_at=? WHERE public_id=? AND status='pending'`,[redirectUrl,expiresAt,now,publicId]);return this.findOwnedByPublicId(publicId);}
  async markCheckoutFailed(publicId:string,now:Date):Promise<void>{await this.#pool.execute(`UPDATE payments SET status='failed',gateway_status='checkout_error',updated_at=? WHERE public_id=? AND status='pending'`,[now,publicId]);}
  async listOwned(userPublicId:string):Promise<PaymentRecord[]>{const[rows]=await this.#pool.execute<PaymentRow[]>(`SELECT ${columns} FROM payments p JOIN users u ON u.id=p.user_id WHERE u.public_id=? ORDER BY p.created_at DESC,p.id DESC`,[userPublicId]);return rows.map(payment);}
  async findOwned(userPublicId:string,publicId:string):Promise<PaymentRecord|null>{const[rows]=await this.#pool.execute<PaymentRow[]>(`SELECT ${columns} FROM payments p JOIN users u ON u.id=p.user_id WHERE u.public_id=? AND p.public_id=? LIMIT 1`,[userPublicId,publicId]);return rows[0]?payment(rows[0]):null;}
  async applyVerifiedNotification(notification:PaymentNotification,payloadHash:string,now:Date):Promise<NotificationOutcome>{const connection=await this.#pool.getConnection();try{await connection.beginTransaction();const[payments]=await connection.execute<(RowDataPacket&{id:number;public_id:string;user_id:number;amount:number|string;status:string;target_plan_code:'basic'|'pro';duration_days_snapshot:number})[]>(`SELECT id,public_id,user_id,amount,status,target_plan_code,duration_days_snapshot FROM payments WHERE merchant_order_id=? LIMIT 1 FOR UPDATE`,[notification.orderId]);const current=payments[0];if(!current){await connection.rollback();return{result:'unknown_order',paymentPublicId:null,paymentStatus:null};}const[events]=await connection.execute<(RowDataPacket&{payload_hash:string})[]>(`SELECT payload_hash FROM payment_events WHERE gateway_event_key=? LIMIT 1 FOR UPDATE`,[notification.eventKey]);if(events[0]){await connection.commit();return{result:events[0].payload_hash===payloadHash?'duplicate':'event_conflict',paymentPublicId:current.public_id,paymentStatus:current.status};}await connection.execute(`INSERT INTO payment_events(payment_id,gateway_event_key,payload_hash,event_type,received_at,processing_status) VALUES(?,?,?,?,?,'received')`,[current.id,notification.eventKey,payloadHash,notification.transactionStatus,now]);if(notification.grossAmount!==`${Number(current.amount).toFixed(2)}`){await connection.execute(`UPDATE payment_events SET processed_at=?,processing_status='rejected',error_message='amount_mismatch' WHERE gateway_event_key=?`,[now,notification.eventKey]);await connection.commit();return{result:'amount_mismatch',paymentPublicId:current.public_id,paymentStatus:current.status};}const success=(notification.transactionStatus==='settlement'||(notification.transactionStatus==='capture'&&notification.fraudStatus==='accept'))&&notification.statusCode==='200';let target:string|null=success?'paid':({deny:'failed',failure:'failed',expire:'expired',cancel:'canceled',refund:'refunded',partial_refund:'refunded'}as Record<string,string>)[notification.transactionStatus]??null;if(current.status==='paid'&&target!=='refunded')target=null;if(target==='paid'&&current.status!=='paid'){const[active]=await connection.execute<(RowDataPacket&{id:number;plan_code:string;ends_at:Date})[]>(`SELECT s.id,p.code plan_code,s.ends_at FROM subscriptions s JOIN plans p ON p.id=s.plan_id WHERE s.user_id=? AND s.status='active' AND s.ends_at>? ORDER BY s.ends_at DESC LIMIT 1 FOR UPDATE`,[current.user_id,now]);let subscriptionId:number;const existing=active[0];if(existing&&existing.plan_code===current.target_plan_code){await connection.execute(`UPDATE subscriptions SET ends_at=DATE_ADD(GREATEST(ends_at,?),INTERVAL ? DAY),updated_at=? WHERE id=?`,[now,current.duration_days_snapshot,now,existing.id]);subscriptionId=existing.id;}else{if(existing)await connection.execute(`UPDATE subscriptions SET status='superseded',updated_at=? WHERE id=?`,[now,existing.id]);const[result]=await connection.execute<ResultSetHeader>(`INSERT INTO subscriptions(public_id,user_id,plan_id,status,starts_at,ends_at,created_at,updated_at) SELECT ?,?,id,'active',?,DATE_ADD(?,INTERVAL ? DAY),?,? FROM plans WHERE code=?`,[randomUUID(),current.user_id,now,now,current.duration_days_snapshot,now,now,current.target_plan_code]);subscriptionId=result.insertId;}await connection.execute(`UPDATE payments SET subscription_id=?,gateway_transaction_id=?,gateway_status=?,fraud_status=?,status='paid',paid_at=?,updated_at=? WHERE id=?`,[subscriptionId,notification.transactionId,notification.transactionStatus,notification.fraudStatus,now,now,current.id]);await connection.execute(`UPDATE cards SET plan_code=?,updated_at=? WHERE user_id=? AND deleted_at IS NULL`,[current.target_plan_code,now,current.user_id]);await connection.execute(`INSERT INTO activity_logs(user_id,event,metadata_text,created_at) VALUES(?,'payment.subscription-activated',?,?)`,[current.user_id,JSON.stringify({paymentPublicId:current.public_id,planCode:current.target_plan_code}),now]);}else if(target){await connection.execute(`UPDATE payments SET gateway_transaction_id=?,gateway_status=?,fraud_status=?,status=?,updated_at=? WHERE id=?`,[notification.transactionId,notification.transactionStatus,notification.fraudStatus,target,now,current.id]);}await connection.execute(`UPDATE payment_events SET processed_at=?,processing_status=? WHERE gateway_event_key=?`,[now,target?'processed':'ignored',notification.eventKey]);await connection.commit();return{result:target?'processed':'ignored',paymentPublicId:current.public_id,paymentStatus:target??current.status};}catch(error){await connection.rollback();throw error;}finally{connection.release();}}
  async findCurrentSubscription(userPublicId:string,now:Date):Promise<CurrentSubscription|null>{const[rows]=await this.#pool.execute<(RowDataPacket&{public_id:string;plan_code:'basic'|'pro';status:string;starts_at:Date;ends_at:Date})[]>(`SELECT s.public_id,p.code plan_code,s.status,s.starts_at,s.ends_at FROM subscriptions s JOIN plans p ON p.id=s.plan_id JOIN users u ON u.id=s.user_id WHERE u.public_id=? AND s.status='active' AND s.starts_at<=? AND s.ends_at>? ORDER BY s.ends_at DESC LIMIT 1`,[userPublicId,now,now]);const row=rows[0];return row?{publicId:row.public_id,planCode:row.plan_code,status:row.status,startsAt:row.starts_at,endsAt:row.ends_at}:null;}
  private async findOwnedByPublicId(publicId:string):Promise<PaymentRecord|null>{const[rows]=await this.#pool.execute<PaymentRow[]>(`SELECT ${columns} FROM payments p WHERE p.public_id=? LIMIT 1`,[publicId]);return rows[0]?payment(rows[0]):null;}
}
