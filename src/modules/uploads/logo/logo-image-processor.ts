import sharp from'sharp';import{AppError}from'../../../shared/http/errors.ts';export class LogoImageProcessor{async process(input:Buffer):Promise<Buffer>{try{const image=sharp(input,{failOn:'warning',limitInputPixels:16_000_000});const m=await image.metadata();if(!m.width||!m.height||!m.format||!['jpeg','png','webp'].includes(m.format)||m.width>4096||m.height>4096)throw new Error('invalid');return await image.rotate().resize(1600,1600,{fit:'inside',withoutEnlargement:true}).webp({quality:85,alphaQuality:90}).toBuffer();}catch{throw new AppError(422,'IMAGE_INVALID','The logo must be a valid JPEG, PNG, or WebP image within dimension limits.');}}}
