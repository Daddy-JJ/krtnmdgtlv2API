import{Router}from'express';import multer from'multer';import{AppError}from'../../../shared/http/errors.ts';import type{LogoController}from'./logo-controller.ts';const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:5*1024*1024,files:1,fields:0}});export function createLogoRouter(c:LogoController):Router{const r=Router();r.post('/:publicId/logo',(req,res,next)=>upload.single('file')(req,res,error=>next(error?new AppError(413,'UPLOAD_TOO_LARGE','The logo upload is invalid or too large.'):undefined)),c.upload);return r;}export function createPublicLogoRouter(c:LogoController):Router{const r=Router();r.get('/:slug/logo',c.publicLogo);return r;}
