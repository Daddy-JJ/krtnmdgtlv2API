import{randomUUID}from'node:crypto';import{mkdir,readFile,unlink,writeFile}from'node:fs/promises';import{resolve}from'node:path';export class LogoFileStorage{readonly #directory:string;constructor(directory:string){this.#directory=directory;}async write(content:Buffer){await mkdir(this.#directory,{recursive:true});const name=`${randomUUID()}.webp`;await writeFile(this.#path(name),content,{flag:'wx',mode:0o640});return name;}async read(name:string){return readFile(this.#path(name));}async remove(name:string|null){if(!name)return;try{await unlink(this.#path(name));}catch(error){if((error as NodeJS.ErrnoException).code!=='ENOENT')throw error;}}#path(name:string){if(!/^[0-9a-f-]{36}\.webp$/.test(name))throw new Error('Invalid logo storage key.');return resolve(this.#directory,name);}}
