import type{CatalogInput,SocialInput}from'../dto/content-input.ts';
export type SocialLink=SocialInput&Readonly<{id:number}>;export type CatalogItem=CatalogInput&Readonly<{publicId:string}>;export type CreateOutcome<T>=T|'limit'|null;
export interface CardContentRepository{listSocial(userId:string,cardId:string):Promise<SocialLink[]|null>;listPublishedSocial(slug:string,limit:number):Promise<SocialLink[]>;createSocial(userId:string,cardId:string,input:SocialInput,limit:number,now:Date):Promise<CreateOutcome<SocialLink>>;updateSocial(userId:string,cardId:string,id:number,input:SocialInput,now:Date):Promise<SocialLink|null>;deleteSocial(userId:string,cardId:string,id:number):Promise<boolean>;listCatalog(userId:string,cardId:string):Promise<CatalogItem[]|null>;listPublishedCatalog(slug:string,limit:number):Promise<CatalogItem[]>;createCatalog(userId:string,cardId:string,input:CatalogInput,limit:number,publicId:string,now:Date):Promise<CreateOutcome<CatalogItem>>;updateCatalog(userId:string,cardId:string,itemId:string,input:CatalogInput,now:Date):Promise<CatalogItem|null>;deleteCatalog(userId:string,cardId:string,itemId:string):Promise<boolean>;}
