import{Router}from'express';import type{CardContentController}from'../controllers/card-content-controller.ts';export function createCardContentRouter(c:CardContentController):Router{const r=Router();r.get('/:publicId/social-links',c.listSocial);r.post('/:publicId/social-links',c.createSocial);r.put('/:publicId/social-links/:linkId',c.updateSocial);r.delete('/:publicId/social-links/:linkId',c.deleteSocial);r.get('/:publicId/catalog-items',c.listCatalog);r.post('/:publicId/catalog-items',c.createCatalog);r.put('/:publicId/catalog-items/:itemId',c.updateCatalog);r.delete('/:publicId/catalog-items/:itemId',c.deleteCatalog);return r;}
